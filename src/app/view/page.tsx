import LoadingScreen from "@/components/LoadingScreen";

export default function newUploadProject() {
    return (
        <div>
            <h1>View the uploaded documents and the found tracelinks here! </h1>
            <LoadingScreen/>
        </div>
    );
}